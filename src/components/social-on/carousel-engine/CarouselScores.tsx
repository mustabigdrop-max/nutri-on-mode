import type { CarouselScore } from "./carousel-engine.types";
export default function CarouselScores({ score }: { score: CarouselScore | null | undefined }) { if(!score)return null; const values=[['CONTEÚDO',score.total_score],['COMPARTILHAR',score.breakdown?.share],['SALVAR',score.breakdown?.save]]; return <section className="grid gap-2 sm:grid-cols-3">{values.map(([label,value])=><div key={label} className="border border-white/10 bg-[#020205] p-4 rounded-none"><div className="font-jarvis text-3xl text-[#00D4FF]">{Number(value)||0}</div><div className="font-tech text-[9px] tracking-[.16em] text-muted-foreground">{label}</div></div>)}{score.top_fix&&<p className="sm:col-span-3 border-l-2 border-[#B8922A] pl-3 text-xs text-muted-foreground">{score.top_fix}</p>}</section>; }
