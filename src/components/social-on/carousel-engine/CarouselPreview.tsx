import { useState } from "react";
import CarouselSlide from "./CarouselSlide";
export default function CarouselPreview({ images, regenerating, onEdit, onRegenerate }: {images:string[];regenerating:number|null;onEdit:()=>void;onRegenerate:(i:number)=>void}) { const [active,setActive]=useState(0); return <div className="space-y-3"><div className="mx-auto max-w-[420px]"><CarouselSlide src={images[active]} index={active} active busy={regenerating===active} onSelect={()=>{}} onEdit={onEdit} onRegenerate={()=>onRegenerate(active)}/></div><div className="grid grid-cols-5 gap-1">{images.map((src,i)=><button key={i} onClick={()=>setActive(i)} className={`border p-1 ${active===i?"border-[#00D4FF]":"border-white/10"}`}><img src={src} alt={`Miniatura ${i+1}`} /></button>)}</div></div>; }
