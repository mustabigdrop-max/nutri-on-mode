import { Clock3, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import type { CarouselHistoryRow } from "./carousel-engine.types";
export default function CarouselHistory({ rows, onOpen, onDelete }: { rows:CarouselHistoryRow[]; onOpen:(r:CarouselHistoryRow)=>void; onDelete:(id:string)=>void }) { return <section className="border border-white/10 p-4 rounded-none"><div className="mb-3 flex items-center gap-2"><Clock3 className="text-[#B8922A]"/><h3 className="font-jarvis uppercase">Histórico do Studio</h3></div>{!rows.length?<p className="text-xs text-muted-foreground">Os carrosséis salvos aparecerão aqui.</p>:<div className="space-y-2">{rows.map(r=><div key={r.id} className="flex items-center justify-between border border-white/10 p-3"><button className="min-w-0 text-left" onClick={()=>onOpen(r)}><div className="truncate text-sm font-semibold">{r.topic}</div><div className="font-tech text-[9px] text-muted-foreground">{r.niche.toUpperCase()} · {new Date(r.created_at).toLocaleDateString("pt-BR")}</div></button><Button size="icon" variant="ghost" onClick={()=>onDelete(r.id)} aria-label="Excluir do histórico"><Trash2/></Button></div>)}</div>}</section>; }
